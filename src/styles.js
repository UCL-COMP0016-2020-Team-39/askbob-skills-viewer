import { makeStyles } from "@material-ui/core";

export default makeStyles(theme => ({
  errorText: {
    backgroundColor: "#fee",
    color: "#f00",
    textAlign: "center",
    marginBottom: "1rem",
  },
  footer: {
    marginBottom: "1rem",
  },
}));
